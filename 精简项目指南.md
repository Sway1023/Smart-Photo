# Immich 项目精简指南

## 目标
移除 AI 功能和移动端，专注于 Web 前后端开发

## 一、可以完全删除的文件夹

### 1. AI/机器学习相关
```bash
# 删除整个 machine-learning 目录
rm -rf machine-learning/

# 说明：包含所有 AI 模型、推理服务代码
# 大小：约 50MB 代码 + 2-5GB 模型缓存
```

### 2. 移动端相关
```bash
# 删除整个 mobile 目录
rm -rf mobile/

# 说明：Flutter 移动应用代码
# 大小：约 100MB+
```

### 3. 端到端测试（可选删除）
```bash
# 删除 e2e 测试目录（如果不需要运行测试）
rm -rf e2e/

# 说明：端到端测试代码，开发时可能不需要
# 大小：约 20MB
```

```bash
# 删除 e2e 认证服务器（测试用）
rm -rf e2e-auth-server/

# 说明：仅用于测试的认证服务器
# 大小：约 5MB
```

### 4. CLI 工具（可选删除）
```bash
# 删除 CLI 工具（如果不需要命令行管理）
rm -rf cli/

# 说明：命令行管理工具
# 大小：约 10MB
```

### 5. 文档（可选删除）
```bash
# 删除文档目录（如果不需要本地构建文档）
rm -rf docs/

# 说明：Docusaurus 文档站点
# 大小：约 50MB
```

### 6. 设计资源（可选删除）
```bash
# 删除设计文件
rm -rf design/

# 说明：Logo、图标等设计资源
# 大小：约 5MB
```

### 7. 国际化文件（可选精简）
```bash
# 如果只需要中文和英文，可以删除其他语言
cd i18n/
# 保留 en.json 和 zh.json，删除其他
rm -f af.json ar.json az.json be.json bg.json bi.json bn.json br.json ca.json cs.json cv.json da.json de.json de_CH.json el.json eo.json es.json et.json eu.json fa.json fi.json fil.json fr.json ga.json gl.json gsw.json gu.json he.json hi.json hr.json hu.json id.json is.json it.json ja.json ko.json lt.json lv.json ml.json ms.json nb.json nl.json nn.json oc.json pl.json pt.json pt_BR.json ro.json ru.json sk.json sl.json sq.json sr.json sv.json ta.json th.json tr.json uk.json vi.json zh_CN.json zh_HK.json zh_TW.json

# 说明：保留需要的语言，删除其他
# 节省：约 5MB
```

### 8. 部署相关（可选删除）
```bash
# 如果不使用 Terraform 部署
rm -rf deployment/

# 说明：云部署配置（Cloudflare 等）
# 大小：约 5MB
```

### 9. Fastlane（移动端相关）
```bash
# 删除 Fastlane 配置
rm -rf fastlane/

# 说明：移动应用发布工具配置
# 大小：约 1MB
```

## 二、需要修改的文件

### 1. Docker Compose 配置

#### docker/docker-compose.yml
```yaml
# 删除或注释掉 immich-machine-learning 服务

name: immich

services:
  immich-server:
    container_name: immich_server
    image: ghcr.io/immich-app/immich-server:${IMMICH_VERSION:-release}
    volumes:
      - ${UPLOAD_LOCATION}:/data
      - /etc/localtime:/etc/localtime:ro
    env_file:
      - .env
    ports:
      - '2283:2283'
    depends_on:
      - redis
      - database
      # 移除 immich-machine-learning 依赖
    restart: always

  # ❌ 删除整个 immich-machine-learning 服务
  # immich-machine-learning:
  #   ...

  redis:
    container_name: immich_redis
    image: docker.io/valkey/valkey:9
    restart: always

  database:
    container_name: immich_postgres
    image: ghcr.io/immich-app/postgres:14
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_DB: ${DB_DATABASE_NAME}
    volumes:
      - ${DB_DATA_LOCATION}:/var/lib/postgresql/data
    restart: always

volumes:
  # ❌ 删除 model-cache 卷
  # model-cache:
```

#### docker/docker-compose.dev.yml
```yaml
# 同样删除 ML 服务配置
```

### 2. 环境变量配置

#### docker/example.env
```bash
# 添加禁用 ML 的配置
UPLOAD_LOCATION=./library
DB_DATA_LOCATION=./postgres
DB_PASSWORD=postgres

# 禁用 AI 功能
IMMICH_MACHINE_LEARNING_ENABLED=false

# 其他配置保持不变
DB_USERNAME=postgres
DB_DATABASE_NAME=immich
```

### 3. 后端代码修改（可选）

如果想彻底移除 ML 相关代码：

#### server/src/services/ 中可以删除的文件
```bash
cd server/src/services/
# 删除 AI 相关服务
rm -f smart-info.service.ts
rm -f smart-info.service.spec.ts
rm -f ocr.service.ts
rm -f ocr.service.spec.ts
rm -f duplicate.service.ts
rm -f duplicate.service.spec.ts
# person.service.ts 包含人脸识别，但也有手动标记功能，建议保留
```

#### server/src/repositories/ 中可以删除的文件
```bash
cd server/src/repositories/
# 删除 ML 仓库
rm -f machine-learning.repository.ts
```

#### server/src/controllers/ 中可以删除的文件
```bash
cd server/src/controllers/
# 删除重复检测控制器
rm -f duplicate.controller.ts
# 人脸控制器建议保留（可以手动管理人脸）
```

### 4. 前端代码修改（可选）

#### 隐藏 AI 相关的 UI 组件

**web/src/routes/admin/system-settings/+page.svelte**
```svelte
<!-- 注释掉 MachineLearningSettings 组件 -->
<script>
  // import MachineLearningSettings from '$lib/components/admin-settings/MachineLearningSettings.svelte';
  
  const settings = [
    // ... 其他设置
    // {
    //   component: MachineLearningSettings,
    //   title: $t('admin.machine_learning_settings'),
    //   ...
    // },
  ];
</script>
```

#### 隐藏搜索相关功能

**web/src/lib/components/search/** 
```bash
# 如果不需要智能搜索，可以简化搜索组件
# 保留基于元数据的搜索（日期、地点、标签等）
# 移除基于 AI 的语义搜索
```

### 5. README 文档修改

#### README.md
```markdown
# 照片管理系统（基于 Immich）

## 功能特性

✅ 保留的功能：
- 照片和视频上传管理
- 相册创建和分享
- 基于元数据的搜索（日期、地点、相机型号）
- 时间线浏览
- 地图展示
- 标签管理
- 用户权限管理
- 回忆功能
- 垃圾箱
- 多用户支持

❌ 移除的功能：
- AI 智能搜索
- 人脸识别
- OCR 文字识别
- 重复照片检测
- 移动端应用

## 系统要求

- 内存：4GB+（不需要 AI 的 6GB）
- CPU：2 核心+
- 存储：根据照片数量而定

## 快速开始

1. 克隆项目
2. 配置 .env 文件
3. 运行 `docker compose up -d`
4. 访问 http://localhost:2283
```

## 三、删除命令脚本

创建一个脚本来自动删除：

```bash
#!/bin/bash
# cleanup.sh - 精简 Immich 项目

echo "开始精简 Immich 项目..."

# 1. 删除 AI/ML 相关
echo "删除 machine-learning 目录..."
rm -rf machine-learning/

# 2. 删除移动端
echo "删除 mobile 目录..."
rm -rf mobile/

# 3. 删除测试相关
echo "删除 e2e 测试..."
rm -rf e2e/
rm -rf e2e-auth-server/

# 4. 删除 CLI
echo "删除 CLI 工具..."
rm -rf cli/

# 5. 删除文档
echo "删除文档目录..."
rm -rf docs/

# 6. 删除设计资源
echo "删除设计资源..."
rm -rf design/

# 7. 删除部署配置
echo "删除部署配置..."
rm -rf deployment/

# 8. 删除 Fastlane
echo "删除 Fastlane..."
rm -rf fastlane/

# 9. 精简国际化文件（保留中英文）
echo "精简国际化文件..."
cd i18n/
find . -type f -name "*.json" ! -name "en.json" ! -name "zh_CN.json" ! -name ".prettierrc" -delete
cd ..

# 10. 删除 ML 相关的硬件加速配置
echo "删除硬件加速配置..."
rm -f docker/hwaccel.ml.yml

echo "✅ 精简完成！"
echo ""
echo "项目大小减少约 200-300MB"
echo ""
echo "接下来需要手动操作："
echo "1. 编辑 docker/docker-compose.yml，删除 immich-machine-learning 服务"
echo "2. 编辑 docker/example.env，添加 IMMICH_MACHINE_LEARNING_ENABLED=false"
echo "3. 更新 README.md 说明移除的功能"
```

## 四、保留的核心目录结构

精简后的项目结构：

```
immich/
├── server/              ✅ 后端核心代码
├── web/                 ✅ 前端 Web 应用
├── docker/              ✅ Docker 配置
├── i18n/                ✅ 国际化（仅中英文）
├── .devcontainer/       ✅ 开发容器配置
├── .github/             ✅ GitHub 配置
├── .vscode/             ✅ VSCode 配置
├── README.md            ✅ 项目说明
├── LICENSE              ✅ 许可证
├── CONTRIBUTING.md      ✅ 贡献指南
└── docker-compose.yml   ✅ 部署配置
```

## 五、验证精简结果

### 1. 检查 Docker 服务
```bash
docker compose config
# 应该只看到 3 个服务：server, database, redis
```

### 2. 检查项目大小
```bash
du -sh .
# 精简前：约 500MB-1GB
# 精简后：约 200-300MB
```

### 3. 启动测试
```bash
docker compose up -d
docker ps
# 应该只有 3 个容器在运行
```

### 4. 功能测试
- ✅ 用户注册登录
- ✅ 照片上传
- ✅ 相册创建
- ✅ 基于元数据的搜索
- ✅ 时间线浏览
- ❌ AI 搜索（应该被禁用或隐藏）
- ❌ 人脸识别（应该被禁用或隐藏）

## 六、Git 操作建议

### 方案 1：创建新分支
```bash
# 创建精简版分支
git checkout -b simplified-version

# 执行删除操作
bash cleanup.sh

# 提交更改
git add .
git commit -m "精简项目：移除 AI 和移动端功能"
```

### 方案 2：Fork 后精简
```bash
# Fork 原项目到你的账号
# 克隆你的 Fork
git clone https://github.com/your-username/immich.git

# 执行精简
bash cleanup.sh

# 提交
git add .
git commit -m "精简项目用于毕业设计"
git push
```

## 七、注意事项

### ⚠️ 删除前备份
```bash
# 创建备份
cp -r immich immich-backup
```

### ⚠️ 数据库迁移
如果已经有数据，删除 ML 功能后：
- 现有的 AI 搜索数据会保留但不可用
- 人脸识别数据会保留但不可用
- 不影响照片、相册等核心数据

### ⚠️ 前端构建
删除后首次构建可能会有错误：
```bash
cd web
npm install
npm run build
# 修复任何导入错误
```

## 八、精简效果对比

| 项目 | 精简前 | 精简后 | 节省 |
|------|--------|--------|------|
| 代码大小 | ~500MB | ~200MB | 60% |
| 容器数量 | 4 个 | 3 个 | 25% |
| 内存需求 | 6-8GB | 4-6GB | 25-33% |
| 启动时间 | ~60s | ~30s | 50% |
| 功能模块 | 30+ | 25+ | - |

## 九、毕业设计说明

在论文中可以这样描述：

> "本系统基于开源项目 Immich 进行二次开发，针对毕业设计需求进行了精简和优化：
> 
> 1. **移除 AI 模块**：考虑到资源限制和研究重点，移除了机器学习相关功能，降低了系统复杂度和资源需求
> 2. **移除移动端**：专注于 Web 端的前后端开发，深入研究 Web 技术栈
> 3. **优化部署**：简化了容器编排，提高了部署效率
> 4. **保留核心功能**：保留了照片管理的所有核心功能，包括上传、相册、分享、搜索、时间线等
> 
> 这种精简使得系统更适合作为毕业设计项目，既保持了功能完整性，又降低了技术复杂度，便于深入研究和创新。"

## 总结

执行以上步骤后，你将得到一个：
- ✅ 专注于 Web 前后端的照片管理系统
- ✅ 资源需求降低 25-50%
- ✅ 代码量减少 60%
- ✅ 仍然保留 25+ 个功能模块可以写进论文
- ✅ 更容易理解和二次开发
