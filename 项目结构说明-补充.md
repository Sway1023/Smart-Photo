# Immich 2.5.6 项目结构深度分析 - 补充文档

> 本文档是《项目结构说明.md》的补充部分

---

## CLI 命令行工具

**路径**: `cli/`  
**技术栈**: TypeScript + Node.js  
**包名**: `@immich/cli`

### 目录结构

#### `cli/src/` - 源代码

##### **index.ts** - CLI 入口
使用 Commander.js 构建命令行界面

##### **commands/** - 命令实现

###### 1. **asset.ts** - 资产管理命令
```bash
immich upload <paths...>     # 上传文件/目录
immich upload --recursive    # 递归上传
immich upload --album <name> # 上传到指定相册
immich upload --skip-hash    # 跳过哈希检查
```

功能：
- 批量上传照片和视频
- 支持递归目录扫描
- 重复检测（基于文件哈希）
- 进度显示
- 断点续传

###### 2. **auth.ts** - 认证命令
```bash
immich login <server-url>    # 登录服务器
immich login-key <api-key>   # 使用 API Key 登录
immich logout                # 登出
```

功能：
- 服务器连接配置
- API Key 管理
- 凭证存储（安全存储）

###### 3. **server-info.ts** - 服务器信息
```bash
immich server-info           # 显示服务器信息
```

功能：
- 服务器版本
- 统计信息
- 配置信息

##### **utils.ts** - 工具函数
- 文件扫描
- 哈希计算
- 进度条显示
- 错误处理

##### **queue.ts** - 上传队列
- 并发控制
- 重试机制
- 队列管理

### 配置文件

#### **package.json**
- 依赖管理
- 脚本定义
- 二进制入口

#### **Dockerfile**
独立的 CLI 容器镜像

#### **vite.config.ts**
使用 Vite 构建 CLI 工具

### 使用场景

1. **批量导入**: 从本地导入大量照片
2. **自动化**: 脚本化上传流程
3. **CI/CD**: 集成到自动化流程
4. **备份**: 定期备份到 Immich

---

## 测试体系

### E2E 测试 (`e2e/`)

**技术栈**: Playwright + Vitest

#### 目录结构

##### `e2e/src/specs/` - 测试规格

###### **server/** - 服务器 API 测试
- `asset.e2e-spec.ts` - 资产 API 测试
- `album.e2e-spec.ts` - 相册 API 测试
- `auth.e2e-spec.ts` - 认证 API 测试
- `search.e2e-spec.ts` - 搜索 API 测试
- `person.e2e-spec.ts` - 人脸识别 API 测试
- `shared-link.e2e-spec.ts` - 分享链接测试

###### **web/** - Web UI 测试
- `login.e2e-spec.ts` - 登录流程测试
- `upload.e2e-spec.ts` - 上传功能测试
- `album.e2e-spec.ts` - 相册操作测试
- `search.e2e-spec.ts` - 搜索功能测试

###### **maintenance/** - 维护模式测试
- `maintenance.e2e-spec.ts` - 维护模式测试

##### `e2e/src/` - 测试工具

- `fixtures.ts` - 测试夹具（用户、资产、相册）
- `generators.ts` - 测试数据生成器
- `utils.ts` - 测试工具函数
- `docker-compose.ts` - Docker 环境管理

#### 配置文件

- `playwright.config.ts` - Playwright 配置
- `vitest.config.ts` - Vitest 配置
- `docker-compose.yml` - 测试环境

### E2E 认证服务器 (`e2e-auth-server/`)

**用途**: 模拟 OAuth 服务器用于测试

#### 文件

- `auth-server.ts` - OAuth 服务器实现
- `startup.ts` - 启动脚本
- `Dockerfile` - 容器镜像

---

## 部署与基础设施

### Docker 配置 (`docker/`)

#### 核心文件

##### **docker-compose.yml** - 基础配置
服务定义：
- `immich-server` - 后端服务
- `immich-machine-learning` - ML 服务
- `postgres` - PostgreSQL 数据库
- `redis` - Redis 缓存

##### **docker-compose.prod.yml** - 生产环境
生产优化：
- 资源限制
- 健康检查
- 重启策略
- 日志配置

##### **docker-compose.dev.yml** - 开发环境
开发特性：
- 热重载
- 调试端口
- 开发工具

##### **docker-compose.rootless.yml** - 无 root 运行
安全配置：
- 非 root 用户
- 权限限制

##### **hwaccel.transcoding.yml** - 硬件加速转码
支持：
- NVIDIA GPU (NVENC/NVDEC)
- Intel Quick Sync (QSV)
- AMD AMF
- VAAPI

##### **hwaccel.ml.yml** - ML 硬件加速
支持：
- CUDA (NVIDIA)
- OpenVINO (Intel)
- CoreML (Apple)

##### **example.env** - 环境变量模板
配置项：
- 数据库连接
- Redis 连接
- 上传路径
- 日志级别
- 机器学习配置

##### **prometheus.yml** - 监控配置
Prometheus 指标采集配置

### 云部署 (`deployment/`)

#### 目录结构

##### `deployment/modules/cloudflare/` - Cloudflare 模块
- Terraform 配置
- DNS 管理
- CDN 配置
- Workers 部署

##### **state.hcl** - Terragrunt 状态配置
远程状态管理

##### **.env** - 部署环境变量
云服务凭证和配置

### 开发容器 (`.devcontainer/`)

#### 配置文件

##### **devcontainer.json** - 主配置
VS Code Dev Container 配置

##### **mobile/** - 移动端开发环境
- Flutter SDK
- Android SDK
- iOS 工具链

##### **server/** - 服务端开发环境
- Node.js
- PostgreSQL
- Redis
- 启动脚本

---

## 文档与资源

### 文档站点 (`docs/`)

**技术栈**: Docusaurus + React

#### 目录结构

##### `docs/docs/` - 文档内容

###### **install/** - 安装指南
- `requirements.md` - 系统要求
- `docker-compose.mdx` - Docker Compose 安装
- `kubernetes.md` - Kubernetes 部署
- `synology.md` - 群晖 NAS 安装
- `unraid.md` - Unraid 安装
- `upgrading.md` - 升级指南

###### **administration/** - 管理指南
- `backup-and-restore.md` - 备份与恢复
- `oauth.md` - OAuth 配置
- `reverse-proxy.md` - 反向代理
- `storage-template.mdx` - 存储模板
- `system-settings.md` - 系统设置
- `user-management.mdx` - 用户管理

###### **features/** - 功能说明
- `facial-recognition.md` - 人脸识别
- `hardware-transcoding.md` - 硬件转码
- `ml-hardware-acceleration.md` - ML 硬件加速
- `searching.md` - 搜索功能
- `libraries.md` - 外部库
- `mobile-backup.md` - 移动端备份

###### **guides/** - 使用指南
- `external-library.md` - 外部库配置
- `remote-machine-learning.md` - 远程 ML 服务
- `scaling-immich.md` - 扩展 Immich
- `database-queries.md` - 数据库查询
- `custom-map-styles.md` - 自定义地图

###### **developer/** - 开发者文档
- `setup.md` - 开发环境搭建
- `architecture.mdx` - 架构说明
- `database-migrations.md` - 数据库迁移
- `testing.md` - 测试指南
- `translations.md` - 翻译贡献

##### `docs/src/` - 文档源码

- `components/` - React 组件
- `css/` - 样式文件
- `pages/` - 自定义页面

##### `docs/static/` - 静态资源

- `img/` - 图片资源
- `fonts/` - 字体文件

#### 配置文件

- `docusaurus.config.js` - Docusaurus 配置
- `sidebars.js` - 侧边栏配置
- `tailwind.config.js` - TailwindCSS 配置

### 国际化 (`i18n/`)

#### 翻译文件 (80+ 种语言)

格式: JSON 键值对

**主要语言**:
- `en.json` - 英语（基准）
- `zh_CN.json` - 简体中文
- `zh_TW.json` - 繁体中文
- `ja.json` - 日语
- `ko.json` - 韩语
- `de.json` - 德语
- `fr.json` - 法语
- `es.json` - 西班牙语

#### 翻译流程

1. 开发者在代码中使用翻译键
2. 提取翻译键到 `en.json`
3. 上传到 Weblate 翻译平台
4. 社区贡献翻译
5. 同步回代码库

### 设计资源 (`design/`)

#### Logo 文件

- `immich-logo.svg` - 主 Logo
- `immich-logo-stacked-light.svg` - 堆叠版（浅色）
- `immich-logo-stacked-dark.svg` - 堆叠版（深色）
- `immich-logo-inline-light.svg` - 内联版（浅色）
- `immich-logo-inline-dark.svg` - 内联版（深色）

#### 其他资源

- `immich-screenshots.png` - 应用截图
- `immich-text-light.png` - 文字 Logo（浅色）
- `immich-text-dark.png` - 文字 Logo（深色）

### 移动应用发布 (`fastlane/`)

#### 元数据

- App Store 描述
- Google Play 描述
- 截图
- 更新日志

---

## 开发指南

### 环境搭建

#### 1. 克隆仓库
```bash
git clone https://github.com/immich-app/immich.git
cd immich
```

#### 2. 安装依赖
```bash
# 安装 pnpm
npm install -g pnpm

# 安装项目依赖
pnpm install
```

#### 3. 启动开发环境
```bash
# 启动 Docker 服务（数据库、Redis）
docker compose -f docker/docker-compose.dev.yml up -d

# 启动后端
cd server
pnpm dev

# 启动前端
cd web
pnpm dev

# 启动机器学习服务
cd machine-learning
python -m immich_ml
```

### 开发工作流

#### 后端开发

1. 修改代码 (`server/src/`)
2. 运行测试 (`pnpm test`)
3. 检查类型 (`pnpm typecheck`)
4. 格式化代码 (`pnpm format`)

#### 前端开发

1. 修改代码 (`web/src/`)
2. 运行测试 (`pnpm test`)
3. 检查类型 (`pnpm check`)
4. 格式化代码 (`pnpm format`)

#### 移动端开发

1. 修改代码 (`mobile/lib/`)
2. 运行测试 (`flutter test`)
3. 代码生成 (`flutter pub run build_runner build`)
4. 格式化代码 (`dart format .`)

### 代码规范

#### TypeScript/JavaScript
- ESLint 规则
- Prettier 格式化
- 严格类型检查

#### Dart
- Dart 分析器
- Dart 格式化器
- Lint 规则

#### Python
- Ruff 格式化和 Lint
- Type hints
- Docstrings

### 提交规范

使用 Conventional Commits:

```
feat: 新功能
fix: 修复 bug
docs: 文档更新
style: 代码格式
refactor: 重构
test: 测试
chore: 构建/工具
```

### 数据库迁移

#### 创建迁移
```bash
cd server
pnpm migration:create <migration-name>
```

#### 运行迁移
```bash
pnpm migration:run
```

### API 更新流程

1. 修改 Controller/DTO
2. 启动服务器
3. 生成 OpenAPI 规范
```bash
pnpm sync:open-api
```
4. 生成客户端代码
```bash
cd open-api
pnpm generate
```

### 测试

#### 单元测试
```bash
# 后端
cd server && pnpm test

# 前端
cd web && pnpm test

# 移动端
cd mobile && flutter test
```

#### E2E 测试
```bash
cd e2e
pnpm test
```

### 构建

#### 生产构建
```bash
# 后端
cd server && pnpm build

# 前端
cd web && pnpm build

# 移动端
cd mobile && flutter build apk
cd mobile && flutter build ios
```

#### Docker 镜像
```bash
docker build -t immich-server -f server/Dockerfile .
docker build -t immich-web -f web/Dockerfile .
docker build -t immich-machine-learning -f machine-learning/Dockerfile .
```

---

## 架构图

### 系统架构

```
┌─────────────┐     ┌─────────────┐
│   Mobile    │     │     Web     │
│     App     │     │   Browser   │
└──────┬──────┘     └──────┬──────┘
       │                   │
       └───────┬───────────┘
               │ HTTPS
       ┌───────▼────────┐
       │   API Server   │
       │   (NestJS)     │
       └───────┬────────┘
               │
       ┌───────┼────────┐
       │       │        │
   ┌───▼───┐ ┌▼──────┐ ┌▼────────────┐
   │ Redis │ │ PG DB │ │ ML Service  │
   └───────┘ └───────┘ │  (FastAPI)  │
                        └─────────────┘
```

### 数据流

```
上传流程:
Mobile/Web → API Server → 存储 → 后台任务队列
                                      │
                                      ▼
                              ┌──────────────┐
                              │ 元数据提取   │
                              │ 缩略图生成   │
                              │ ML 处理      │
                              └──────────────┘
```

---

## 相关链接

- 📖 [官方文档](https://docs.immich.app/)
- 🌐 [官方网站](https://immich.app/)
- 💬 [Discord 社区](https://discord.immich.app)
- 🐛 [问题追踪](https://github.com/immich-app/immich/issues)
- 🗺️ [开发路线图](https://immich.app/roadmap)
- 🎮 [在线演示](https://demo.immich.app)

---

**文档版本**: 基于 Immich 2.5.6  
**最后更新**: 2026-03-18
