# Immich 2.5.6 项目结构深度分析

> 高性能自托管照片和视频管理解决方案 - 完整架构与模块详解

## 📋 目录

1. [项目概述](#项目概述)
2. [Server 后端服务](#server-后端服务)
3. [Web 前端应用](#web-前端应用)
4. [Mobile 移动端应用](#mobile-移动端应用)
5. [Machine Learning 机器学习服务](#machine-learning-机器学习服务)
6. [CLI 命令行工具](#cli-命令行工具)
7. [测试体系](#测试体系)
8. [部署与基础设施](#部署与基础设施)
9. [文档与资源](#文档与资源)

---

## 项目概述

**Immich** 是一个基于 Monorepo 架构的全栈应用，提供照片和视频的自托管管理解决方案。

### 基本信息
- **版本**: 2.5.6
- **包管理器**: pnpm@10.30.3
- **许可证**: AGPL v3
- **架构**: Monorepo + 微服务
- **主要语言**: TypeScript (后端/前端), Dart (移动端), Python (机器学习)

### 技术栈概览
| 模块 | 技术栈 | 数据库/存储 |
|------|--------|------------|
| 后端 API | NestJS + TypeScript | PostgreSQL + Redis |
| Web 前端 | Svelte + Vite | LocalStorage |
| 移动端 | Flutter + Dart | Isar (本地数据库) |
| 机器学习 | FastAPI + Python | 模型文件 + 向量数据库 |
| CLI 工具 | Node.js + TypeScript | - |

---


## Server 后端服务

**路径**: `server/`  
**技术栈**: TypeScript + NestJS + PostgreSQL + Redis  
**启动文件**: `src/main.ts`

### 核心架构

Server 采用 **Worker 多进程架构**，通过 `main.ts` 启动管理器，根据配置启动不同的 Worker：

- **API Worker** (`workers/api.ts`): 处理 HTTP 请求，使用 Child Process
- **Microservices Worker** (`workers/microservices.ts`): 后台任务处理，使用 Worker Thread
- **Maintenance Worker** (`workers/maintenance.ts`): 维护模式专用

### 目录结构详解

#### 1. **controllers/** - 控制器层 (50+ 个控制器)
负责处理 HTTP 请求路由和响应

**核心控制器**:
- `asset.controller.ts` - 资产上传、查询、更新
- `asset-media.controller.ts` - 媒体文件流式传输、缩略图
- `album.controller.ts` - 相册 CRUD 操作
- `auth.controller.ts` - 用户认证、登录、注册
- `search.controller.ts` - 搜索功能（元数据、智能搜索）
- `person.controller.ts` - 人脸识别相关
- `user.controller.ts` / `user-admin.controller.ts` - 用户管理
- `shared-link.controller.ts` - 分享链接管理
- `library.controller.ts` - 外部库管理
- `job.controller.ts` - 后台任务管理
- `system-config.controller.ts` - 系统配置

**特殊控制器**:
- `oauth.controller.ts` - OAuth 认证集成
- `map.controller.ts` - 地图数据
- `memory.controller.ts` - 回忆功能
- `timeline.controller.ts` - 时间线视图
- `trash.controller.ts` - 回收站
- `duplicate.controller.ts` - 重复检测
- `stack.controller.ts` - 照片堆叠
- `workflow.controller.ts` - 工作流管理

#### 2. **services/** - 业务逻辑层 (50+ 个服务)
实现具体业务逻辑，每个 Controller 对应一个或多个 Service

**核心服务**:
- `asset.service.ts` - 资产管理核心逻辑
- `metadata.service.ts` - EXIF 元数据提取和处理
- `storage.service.ts` - 文件存储管理
- `search.service.ts` - 搜索引擎集成
- `smart-info.service.ts` - 智能信息（CLIP、对象检测）
- `media.service.ts` - 媒体转码、缩略图生成
- `job.service.ts` - 后台任务调度
- `library.service.ts` - 外部库扫描和同步

**特色服务**:
- `person.service.ts` - 人脸识别和聚类
- `partner.service.ts` - 伙伴分享
- `notification.service.ts` - 通知系统
- `ocr.service.ts` - 光学字符识别
- `duplicate.service.ts` - 重复资产检测
- `database-backup.service.ts` - 数据库备份

#### 3. **repositories/** - 数据访问层 (40+ 个仓库)
封装数据库操作，使用 Kysely 查询构建器

**主要仓库**:
- `asset.repository.ts` - 资产数据访问
- `album.repository.ts` - 相册数据访问
- `user.repository.ts` - 用户数据访问
- `person.repository.ts` - 人脸数据访问
- `machine-learning.repository.ts` - ML 服务通信
- `search.repository.ts` - 搜索索引操作
- `storage.repository.ts` - 文件系统操作
- `email.repository.ts` - 邮件发送
- `job.repository.ts` - 任务队列（BullMQ）

#### 4. **queries/** - SQL 查询文件 (30+ 个 .sql 文件)
复杂 SQL 查询的独立文件，提高可维护性

示例:
- `asset.repository.sql` - 资产查询
- `search.repository.sql` - 全文搜索
- `person.repository.sql` - 人脸查询
- `album.repository.sql` - 相册关联查询

#### 5. **dtos/** - 数据传输对象 (40+ 个 DTO)
定义 API 请求和响应的数据结构

**主要 DTO**:
- `asset.dto.ts` - 资产相关 DTO
- `auth.dto.ts` - 认证相关 DTO
- `album.dto.ts` - 相册相关 DTO
- `search.dto.ts` - 搜索参数 DTO
- `system-config.dto.ts` - 系统配置 DTO
- `user.dto.ts` - 用户信息 DTO

#### 6. **schema/** - 数据库模式
- `schema/tables/` - 数据库表定义（Kysely）
- `schema/migrations/` - 数据库迁移文件
- `schema/enums.ts` - 枚举类型定义
- `schema/functions.ts` - 数据库函数

#### 7. **middleware/** - 中间件
- `auth.guard.ts` - 认证守卫
- `asset-upload.interceptor.ts` - 文件上传拦截器
- `error.interceptor.ts` - 错误处理
- `logging.interceptor.ts` - 日志记录
- `websocket.adapter.ts` - WebSocket 适配器

#### 8. **commands/** - CLI 命令
- `grant-admin.ts` - 授予管理员权限
- `reset-admin-password.command.ts` - 重置管理员密码
- `list-users.command.ts` - 列出用户
- `oauth-login.ts` - OAuth 登录
- `maintenance-mode.ts` - 维护模式切换

#### 9. **emails/** - 邮件模板 (React Email)
- `welcome.email.tsx` - 欢迎邮件
- `album-invite.email.tsx` - 相册邀请
- `album-update.email.tsx` - 相册更新通知

#### 10. **workers/** - Worker 进程
- `api.ts` - API 服务器 Worker
- `microservices.ts` - 后台任务 Worker
- `maintenance.ts` - 维护模式 Worker

#### 11. **utils/** - 工具函数
- `database.ts` - 数据库连接工具
- `mime-types.ts` - MIME 类型处理
- `file.ts` - 文件操作
- `logger.ts` - 日志工具
- `transform.ts` - 数据转换

### 关键特性

1. **多进程架构**: API 和后台任务分离，提高性能
2. **任务队列**: 使用 BullMQ 处理异步任务
3. **数据库迁移**: 自动化数据库版本管理
4. **类型安全**: 全面使用 TypeScript 和 Kysely
5. **测试覆盖**: 每个服务都有对应的 `.spec.ts` 测试文件

---


## Web 前端应用

**路径**: `web/`  
**技术栈**: Svelte + SvelteKit + Vite + TailwindCSS  
**构建工具**: Vite

### 目录结构详解

#### `web/src/` - 源代码根目录

##### 1. **routes/** - SvelteKit 路由 (基于文件系统)

**路由结构**:
```
routes/
├── (user)/              # 用户区域（需要认证）
│   ├── photos/          # 照片浏览
│   ├── albums/          # 相册管理
│   ├── people/          # 人物管理
│   ├── map/             # 地图视图
│   ├── memories/        # 回忆
│   ├── archive/         # 归档
│   ├── favorites/       # 收藏
│   ├── trash/           # 回收站
│   └── sharing/         # 分享管理
├── admin/               # 管理员区域
│   ├── users/           # 用户管理
│   ├── jobs/            # 任务管理
│   ├── system-settings/ # 系统设置
│   └── server-stats/    # 服务器统计
├── auth/                # 认证相关
│   ├── login/           # 登录页
│   ├── register/        # 注册页
│   └── oauth/           # OAuth 回调
├── link/                # 公共分享链接
└── maintenance/         # 维护模式页面
```

**特殊文件**:
- `+layout.svelte` - 全局布局
- `+layout.ts` - 布局数据加载
- `+page.svelte` - 页面组件
- `+page.ts` - 页面数据加载
- `+error.svelte` - 错误页面

##### 2. **lib/** - 共享库

###### **lib/components/** - UI 组件库
组件按功能分类：

- `album-page/` - 相册页面组件
- `asset-viewer/` - 资产查看器（照片/视频播放器）
- `photos-page/` - 照片页面组件
- `admin-page/` - 管理页面组件
- `shared-components/` - 通用组件
- `map-page/` - 地图组件
- `memory-page/` - 回忆组件
- `user-settings-page/` - 用户设置组件

**核心组件**:
- `asset-grid/` - 虚拟滚动照片网格
- `gallery-viewer/` - 画廊查看器
- `search-bar/` - 搜索栏
- `side-bar/` - 侧边栏导航
- `upload-panel/` - 上传面板

###### **lib/stores/** - Svelte Stores (状态管理)
- `assets.store.ts` - 资产状态
- `album.store.ts` - 相册状态
- `user.store.ts` - 用户状态
- `preferences.store.ts` - 用户偏好
- `websocket.store.ts` - WebSocket 连接
- `upload.store.ts` - 上传状态
- `search.store.ts` - 搜索状态

###### **lib/services/** - 服务层
封装 API 调用：

- `api/` - API 客户端（基于 OpenAPI 生成）
- `asset.service.ts` - 资产服务
- `album.service.ts` - 相册服务
- `auth.service.ts` - 认证服务
- `search.service.ts` - 搜索服务
- `upload.service.ts` - 上传服务
- `websocket.service.ts` - WebSocket 服务

###### **lib/utils/** - 工具函数
- `asset-utils.ts` - 资产处理工具
- `date-utils.ts` - 日期格式化
- `file-utils.ts` - 文件处理
- `thumbnail-utils.ts` - 缩略图工具
- `navigation.ts` - 导航工具
- `handle-error.ts` - 错误处理

###### **lib/actions/** - Svelte Actions
自定义 DOM 行为：
- `click-outside.ts` - 点击外部检测
- `intersection-observer.ts` - 可见性检测
- `shortcut.ts` - 键盘快捷键

###### **lib/modals/** - 模态对话框
- `create-album-modal.svelte` - 创建相册
- `share-modal.svelte` - 分享对话框
- `settings-modal.svelte` - 设置对话框
- `confirm-dialog.svelte` - 确认对话框

##### 3. **static/** - 静态资源
- `fonts/` - 字体文件
- `images/` - 图片资源
- `favicon.png` - 网站图标

##### 4. **service-worker/** - Service Worker
- `index.ts` - Service Worker 主文件
- `messaging.ts` - 消息处理
- `request.ts` - 请求拦截

##### 5. **hooks/** - SvelteKit Hooks
- `hooks.client.ts` - 客户端钩子（错误处理、导航）
- `hooks.server.ts` - 服务端钩子（认证、日志）

##### 6. **params/** - 路由参数匹配器
- `id.ts` - ID 参数验证
- `photos.ts` - 照片路由参数

### 关键特性

1. **虚拟滚动**: 高性能照片网格，支持数万张照片
2. **渐进式 Web 应用**: Service Worker 支持离线访问
3. **响应式设计**: 适配桌面和移动设备
4. **实时更新**: WebSocket 实时同步
5. **国际化**: 支持多语言切换
6. **主题系统**: 深色/浅色模式

### 构建配置

- `vite.config.ts` - Vite 配置
- `svelte.config.js` - Svelte 配置
- `tailwind.config.js` - TailwindCSS 配置
- `tsconfig.json` - TypeScript 配置

---


## Mobile 移动端应用

**路径**: `mobile/`  
**技术栈**: Flutter + Dart + Riverpod  
**本地数据库**: Isar + Drift  
**平台**: iOS 和 Android

### 目录结构详解

#### `mobile/lib/` - Dart 源代码

##### 1. **main.dart** - 应用入口
启动流程：
1. 初始化数据库（Isar + Drift）
2. 初始化本地化（EasyLocalization）
3. 配置 Worker Manager（多线程）
4. 启动应用（ProviderScope）
5. 设置后台服务

##### 2. **pages/** - 页面组件
按功能模块组织：

- `photos/` - 照片浏览页面
  - `photos_page.dart` - 主照片页
  - `asset_viewer_page.dart` - 资产查看器
- `albums/` - 相册列表页面
- `album/` - 单个相册详情页面
- `search/` - 搜索页面
- `backup/` - 备份设置页面
- `settings/` - 设置页面
  - `settings_page.dart` - 设置主页
  - `backup_settings_page.dart` - 备份设置
  - `theme_settings_page.dart` - 主题设置
- `login/` - 登录页面
- `onboarding/` - 引导页面
- `library/` - 库管理页面
- `editing/` - 照片编辑页面
- `share_intent/` - 分享意图处理

##### 3. **providers/** - Riverpod 状态管理
Flutter 使用 Riverpod 进行状态管理：

**核心 Providers**:
- `asset.provider.dart` - 资产状态
- `album/` - 相册相关 Providers
  - `album.provider.dart` - 相册列表
  - `album_detail.provider.dart` - 相册详情
- `backup/` - 备份相关 Providers
  - `backup.provider.dart` - 备份状态
  - `backup_verification.provider.dart` - 备份验证
- `auth.provider.dart` - 认证状态
- `user.provider.dart` - 用户信息
- `timeline/` - 时间线 Providers
- `search/` - 搜索 Providers
- `image/` - 图片加载 Providers
- `map/` - 地图 Providers

**基础设施 Providers**:
- `db.provider.dart` - 数据库实例
- `api.provider.dart` - API 客户端
- `network.provider.dart` - 网络状态
- `secure_storage.provider.dart` - 安全存储
- `app_settings.provider.dart` - 应用设置

##### 4. **services/** - 业务逻辑服务 (40+ 个服务)

**核心服务**:
- `asset.service.dart` - 资产管理
- `backup.service.dart` - 备份核心逻辑
- `background_upload.service.dart` - 后台上传
- `sync.service.dart` - 同步服务
- `album.service.dart` - 相册管理
- `auth.service.dart` - 认证服务
- `search.service.dart` - 搜索服务

**平台服务**:
- `device.service.dart` - 设备信息
- `local_notification.service.dart` - 本地通知
- `background.service.dart` - 后台服务
- `gcast.service.dart` - Chromecast 投屏
- `share_intent_service.dart` - 分享意图

**工具服务**:
- `hash.service.dart` - 文件哈希计算
- `exif.service.dart` - EXIF 数据提取
- `download.service.dart` - 下载管理
- `local_files_manager.service.dart` - 本地文件管理

##### 5. **repositories/** - 数据访问层 (30+ 个仓库)

**API 仓库**:
- `asset_api.repository.dart` - 资产 API
- `album_api.repository.dart` - 相册 API
- `auth_api.repository.dart` - 认证 API
- `person_api.repository.dart` - 人物 API

**本地仓库**:
- `asset.repository.dart` - 本地资产数据
- `album.repository.dart` - 本地相册数据
- `database.repository.dart` - 数据库操作
- `secure_storage.repository.dart` - 安全存储

**媒体仓库**:
- `asset_media.repository.dart` - 资产媒体访问
- `album_media.repository.dart` - 相册媒体访问
- `file_media.repository.dart` - 文件媒体访问

##### 6. **entities/** - 数据实体 (Isar 数据库)
使用 Isar 作为本地 NoSQL 数据库：

- `asset.entity.dart` - 资产实体
- `album.entity.dart` - 相册实体
- `backup_album.entity.dart` - 备份相册
- `device_asset.entity.dart` - 设备资产
- `ios_device_asset.entity.dart` - iOS 设备资产
- `android_device_asset.entity.dart` - Android 设备资产
- `etag.entity.dart` - ETag 缓存
- `duplicated_asset.entity.dart` - 重复资产
- `store.entity.dart` - 键值存储

每个实体都有对应的 `.g.dart` 生成文件（Isar 代码生成）

##### 7. **models/** - 数据模型
按功能分类：

- `albums/` - 相册模型
- `auth/` - 认证模型
- `backup/` - 备份模型
- `search/` - 搜索模型
- `map/` - 地图模型
- `memories/` - 回忆模型
- `activities/` - 活动模型
- `upload/` - 上传模型
- `download/` - 下载模型

##### 8. **widgets/** - UI 组件
可复用的 UI 组件：

- `asset_grid/` - 资产网格组件
- `asset_viewer/` - 资产查看器组件
- `album/` - 相册组件
- `backup/` - 备份组件
- `common/` - 通用组件
- `map/` - 地图组件
- `memories/` - 回忆组件
- `search/` - 搜索组件
- `settings/` - 设置组件
- `photo_view/` - 照片查看组件

##### 9. **extensions/** - Dart 扩展
为现有类型添加便捷方法：

- `build_context_extensions.dart` - BuildContext 扩展
- `datetime_extensions.dart` - DateTime 扩展
- `string_extensions.dart` - String 扩展
- `asset_extensions.dart` - Asset 扩展
- `collection_extensions.dart` - 集合扩展
- `theme_extensions.dart` - 主题扩展

##### 10. **utils/** - 工具函数

- `bootstrap.dart` - 应用启动初始化
- `migration.dart` - 数据迁移
- `image_url_builder.dart` - 图片 URL 构建
- `hash.dart` - 哈希计算
- `debounce.dart` / `throttle.dart` - 防抖/节流
- `async_mutex.dart` - 异步互斥锁
- `backup_progress.dart` - 备份进度计算
- `hooks/` - Flutter Hooks
- `cache/` - 缓存工具

##### 11. **platform/** - 平台通道 (Pigeon)
使用 Pigeon 生成的平台通道代码：

- `background_worker_api.g.dart` - 后台 Worker API
- `local_image_api.g.dart` - 本地图片 API
- `thumbnail_api.g.dart` - 缩略图 API
- `network_api.g.dart` - 网络 API
- `connectivity_api.g.dart` - 连接性 API

##### 12. **routing/** - 路由管理
使用 AutoRoute 进行路由管理：

- `router.dart` - 路由配置
- `router.gr.dart` - 生成的路由代码
- `auth_guard.dart` - 认证守卫
- `backup_permission_guard.dart` - 备份权限守卫
- `gallery_guard.dart` - 相册权限守卫

##### 13. **theme/** - 主题系统
- `theme_data.dart` - 主题数据
- `color_scheme.dart` - 颜色方案
- `dynamic_theme.dart` - 动态主题（Material You）

##### 14. **constants/** - 常量定义
- `constants.dart` - 通用常量
- `colors.dart` - 颜色常量
- `enums.dart` - 枚举定义
- `locales.dart` - 语言环境
- `errors.dart` - 错误常量

#### 其他重要目录

##### `mobile/android/` - Android 原生代码
- `app/src/main/kotlin/` - Kotlin 代码
- `app/build.gradle` - 构建配置
- AndroidManifest.xml - 应用清单

##### `mobile/ios/` - iOS 原生代码
- `Runner/` - Swift 代码
- `Podfile` - CocoaPods 依赖
- Info.plist - 应用配置

##### `mobile/openapi/` - OpenAPI 客户端
从服务器 OpenAPI 规范生成的 Dart 客户端

##### `mobile/pigeon/` - Pigeon 定义
平台通道接口定义

### 关键特性

1. **自动备份**: 后台自动上传照片和视频
2. **离线优先**: 本地数据库缓存，支持离线浏览
3. **增量同步**: 只同步变更的数据
4. **多线程**: Worker Manager 处理耗时任务
5. **平台集成**: 深度集成 iOS/Android 系统功能
6. **Material Design 3**: 支持动态颜色（Android 12+）

### 构建配置

- `pubspec.yaml` - Flutter 依赖配置
- `build.yaml` - 代码生成配置
- `analysis_options.yaml` - 代码分析规则

---


## Machine Learning 机器学习服务

**路径**: `machine-learning/`  
**技术栈**: Python + FastAPI + PyTorch + ONNX  
**端口**: 默认 3003

### 目录结构详解

#### `machine-learning/immich_ml/` - 主应用目录

##### 1. **main.py** - FastAPI 应用入口
定义 API 端点：
- `/ping` - 健康检查
- `/predict` - 通用预测接口
- `/clip/encode-image` - CLIP 图像编码
- `/clip/encode-text` - CLIP 文本编码
- `/facial-recognition/detect-faces` - 人脸检测
- `/facial-recognition/recognize` - 人脸识别
- `/object-detection/detect` - 物体检测
- `/ocr/recognize` - 文字识别

##### 2. **models/** - 模型管理
模型加载和推理逻辑：

**模型类型**:
- `clip/` - CLIP 模型（语义搜索）
  - 图像编码器
  - 文本编码器
  - 支持多种 CLIP 变体
- `facial_recognition/` - 人脸识别
  - 人脸检测模型（MTCNN/RetinaFace）
  - 人脸特征提取（ArcFace/FaceNet）
- `object_detection/` - 物体检测
  - YOLO 系列模型
  - 目标分类和定位
- `ocr/` - 光学字符识别
  - 文本检测
  - 文本识别

**模型文件**:
- `base.py` - 模型基类
- `cache.py` - 模型缓存管理
- `constants.py` - 模型常量（路径、配置）

##### 3. **sessions/** - 推理会话管理
- `session.py` - ONNX Runtime 会话管理
- `providers.py` - 执行提供者（CPU/CUDA/OpenVINO）

##### 4. **schemas.py** - 数据模型
Pydantic 模型定义：
- 请求模型（输入验证）
- 响应模型（输出格式）
- 配置模型

##### 5. **config.py** - 配置管理
环境变量和配置：
- 模型路径
- 缓存设置
- 硬件加速配置
- 日志级别

##### 6. **gunicorn_conf.py** - Gunicorn 配置
生产环境 WSGI 服务器配置：
- Worker 数量
- 超时设置
- 日志配置

#### `machine-learning/ann/` - 近似最近邻搜索

##### **ann.cpp** - C++ 实现
高性能向量搜索：
- HNSW（Hierarchical Navigable Small World）算法
- 用于快速相似度搜索
- Python 绑定

##### **build.sh** - 编译脚本
编译 C++ 扩展模块

##### **export/** - 导出工具
模型导出和转换工具

#### `machine-learning/scripts/` - 辅助脚本

- `configure-apt.sh` - APT 包管理配置
- `healthcheck.py` - 健康检查脚本

#### 配置文件

##### **pyproject.toml** - Python 项目配置
使用 UV 包管理器：
- 依赖声明
- 项目元数据
- 构建配置

##### **Dockerfile** - 容器镜像
多阶段构建：
1. 基础镜像（Python + 系统依赖）
2. 模型下载
3. 应用安装
4. 运行时配置

支持的硬件加速：
- CUDA（NVIDIA GPU）
- OpenVINO（Intel）
- CoreML（Apple Silicon）

##### **locustfile.py** - 性能测试
使用 Locust 进行负载测试

##### **test_main.py** - 单元测试
FastAPI 端点测试

### 工作流程

#### 1. **模型初始化**
```
启动 → 加载配置 → 下载模型 → 初始化 ONNX Runtime → 预热模型
```

#### 2. **推理流程**
```
接收请求 → 验证输入 → 预处理 → 模型推理 → 后处理 → 返回结果
```

#### 3. **模型缓存**
- 首次加载模型到内存
- 后续请求复用已加载模型
- LRU 缓存策略

### 支持的模型

#### CLIP 模型
- `openai/clip-vit-base-patch32`
- `openai/clip-vit-large-patch14`
- 自定义 CLIP 变体

#### 人脸识别模型
- 检测: MTCNN, RetinaFace
- 识别: ArcFace, FaceNet

#### 物体检测模型
- YOLOv5
- YOLOv8
- EfficientDet

#### OCR 模型
- PaddleOCR
- EasyOCR

### 硬件加速

#### CUDA (NVIDIA GPU)
```dockerfile
FROM nvidia/cuda:11.8.0-cudnn8-runtime-ubuntu22.04
```

#### OpenVINO (Intel)
```python
providers = ['OpenVINOExecutionProvider', 'CPUExecutionProvider']
```

#### CoreML (Apple Silicon)
```python
providers = ['CoreMLExecutionProvider', 'CPUExecutionProvider']
```

### 性能优化

1. **模型量化**: INT8/FP16 量化减少内存占用
2. **批处理**: 批量处理多个请求
3. **异步处理**: FastAPI 异步端点
4. **模型缓存**: 避免重复加载
5. **向量索引**: HNSW 快速搜索

### API 示例

#### CLIP 图像编码
```python
POST /clip/encode-image
{
  "image": "base64_encoded_image",
  "model": "openai/clip-vit-base-patch32"
}
```

#### 人脸检测
```python
POST /facial-recognition/detect-faces
{
  "image": "base64_encoded_image",
  "min_score": 0.7
}
```

---

